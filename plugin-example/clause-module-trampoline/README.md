# Delegated clause producer

Calls another clause module through the typed handle. Actual host calls validate advertised schemas.

See the [workspace guide](../README.md) for Cargo build and test commands,
funding assumptions, and the complete executable catalog. This module has a
[representative input](../../contrib/vectors/examples/clause-module-trampoline.json).

Coverage: Catalog calls the actual clause producer.
