# batching-trait

Versioned payment payloads and typed batching handles used by TreePay and TrampolinePay.

This is an interface library, not an executable WASM guest. See the
[workspace guide](../README.md) for consumers, wire conventions, and tests.
