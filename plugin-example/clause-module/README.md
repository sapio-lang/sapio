# Clause producer

Returns a clause requiring both supplied keys. Its result is a clause string rather than a compiled contract.

See the [workspace guide](../README.md) for Cargo build and test commands,
funding assumptions, and the complete executable catalog. This module has a
[representative input](../../contrib/vectors/examples/clause-module.json).

Coverage: Catalog checks the exact clause.
