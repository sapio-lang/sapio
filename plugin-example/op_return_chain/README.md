# Data chain

Appends OP_RETURN data or closes to its owner. Continuation fees come from the available balance.

See the [workspace guide](../README.md) for Cargo build and test commands,
funding assumptions, and the complete executable catalog. This module has a
[representative input](../../contrib/vectors/examples/op_return_chain.json).

Coverage: Catalog plus library continuation tests.
