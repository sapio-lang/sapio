# Federated peg

Provides normal and delayed recovery threshold paths. Keys and thresholds are contract parameters; external chain verification is outside this example.

See the [workspace guide](../README.md) for Cargo build and test commands,
funding assumptions, and the complete executable catalog. This module has a
[representative input](../../contrib/vectors/examples/fedpeg.json).

Coverage: Catalog plus library federation tests.
